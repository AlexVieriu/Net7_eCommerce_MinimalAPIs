@page "/logout"

@inject IAuthentificationRepository auth
@inject NavigationManager navbar

@code{
    protected override async  Task OnInitializedAsync()
    {
        await auth.Logout();
        navbar.NavigateTo("/products");
    }
}